import React from 'react';
import PropTypes from 'prop-types';

const Header = ({}) => {
  return (
    <div>header ...</div>
  );
}

Header.propTypes = {
  // : PropTypes.
};

export default Header;
